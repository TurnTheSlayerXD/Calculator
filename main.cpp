#include "Building_Functions.h"
using namespace std;

int main() {
    string expr;
    string polynom;
    int* res;
    while(	getline(cin, expr)) {
        //Polynom A;
        cout<<count(expr)<<"\n";
       // res = A.create_listOFvalues(expr);
        //polynom = A.build_polynom(res, A.symbols.size());
        //cout << polynom<<"\n";
       // delete []res;
    }
	return 0;
}